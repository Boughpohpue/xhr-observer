# xhr-observer
A lightweight JavaScript library for intercepting and observing XMLHttpRequest traffic, allowing requests to be matched by HTTP method and URL pattern and handled through a centralized, extensible API.
